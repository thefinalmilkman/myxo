Define three functions:
- `adder(n)` — returns a closure that adds `n` to its argument.
- `greet(name, greeting="Hello")` — returns `"<greeting>, <name>"`, using a default parameter.
- `count(*items)` — returns how many arguments it received, using `*args` rest parameters.