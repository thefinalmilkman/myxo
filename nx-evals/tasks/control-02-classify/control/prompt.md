Define a function `sign_word(n)` using a `match` statement that classifies a number and returns a string:
- `"zero"` when `n` is exactly `0`
- `"one"` when `n` is exactly `1`
- `"neg-one"` when `n` is exactly `-1`
- `"other"` for any other number

Use a `match`/`case` statement.