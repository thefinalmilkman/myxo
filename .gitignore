# Node
node_modules/

# Eval run artifacts — regenerated by `node myxo-evals/run-evals.js`; baseline numbers live in myxo-evals/BASELINE.md (committed)
myxo-evals/results/*.jsonl

# OS / editor
.DS_Store
*.swp

# Python bytecode (examples get compiled when hosts embed them)
__pycache__/
*.pyc
.venv/

# npm pack artifacts
*.tgz

# Lane locks (multi-session yield sign — see AGENTS.md)
.lane/
